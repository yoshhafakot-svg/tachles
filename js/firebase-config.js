/**
 * Firebase / API configuration
 *
 * After deploying Cloud Functions, set apiUrl to your function URL,
 * or leave as "/api/leads" when using Firebase Hosting rewrite.
 */
window.APP_CONFIG = {
  apiUrl: "/api/leads"
};
